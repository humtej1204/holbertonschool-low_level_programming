#include <stdio.h>
/**
 * main - main function
 *
 * Return: end the program
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; ++c)
		printf("%c", c);

	if (c == e || c == q)
		continue;

	printf("\n");

	return (0);
}
