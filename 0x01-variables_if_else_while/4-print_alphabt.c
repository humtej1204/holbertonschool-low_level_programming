#include <stdio.h>
/**
 * main - main function
 *
 * Return: end the program
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'a'; ++c)
		printf("%c", c);

	if (c == e || c == q)
		continue;

	return (0);
}
